const Query = require('./Query')

class DELETE extends Query {

    static in (_context) {
        return { from (entity) {
            const q = (new DELETE) .in (_context)
            q._cqn.from = q.target (entity)
            return q
        }}
    }

    valueOf(){ return super.valueOf('DELETE from') }
}

module.exports = DELETE
