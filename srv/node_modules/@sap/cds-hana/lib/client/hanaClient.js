let error

const _getClient = name => {
  try {
    return require(name)
  } catch (err) {
    if (name === 'hdb') {
      error = err
      return _getClient('@sap/hana-client')
    }

    throw error
  }
}

module.exports = _getClient('hdb')
